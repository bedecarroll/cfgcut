rules:
  excessive-permissions:
    ignore:
      - release.yml:18
  template-injection:
    ignore:
      - release.yml:80
      - release.yml:131
      - release.yml:141
      - release.yml:145
  unpinned-images:
    ignore:
      # Generated by cargo-dist; the image comes from dist's computed matrix.
      - release.yml:111
